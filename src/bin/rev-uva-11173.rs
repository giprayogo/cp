/// Reverse of uva-11173.rs
// use std::io;
use std::error;

fn main() -> Result<(), Box<dyn error::Error>> {
    // TODO: not finished
    // let mut buf = String::new();
    // let stdin = io::stdin();
    
    Ok(())
}